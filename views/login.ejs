<div class="bg-blue-500 h-screen w-screen flex flex-col items-center justify-center">
    <form action="" method="POST" class="bg-slate-50 rounded flex flex-col items-center justify-center gap-3 px-7 py-6 shadow-xl w-80">
        <h1 class="font-bold text-xl self-start mb-2">Login</h1>
        <div class="flex flex-col gap-1 w-full">
            <label for="username" class="font-semibold text-sm">Username</label>
            <input type="text" name="username" placeholder="Username" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline">
        </div>
        <div class="flex flex-col gap-1 w-full mb-4">
            <label for="password" class="font-semibold text-sm">Password</label>
            <input type="password" name="password" placeholder="Password" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline">
            <% if (error) { %>
                <span class="text-red-500 text-xs"><%= error %></span>
            <% } %>
        </div>
        <button type="submit" class="w-full px-5 py-2 bg-blue-500 text-white rounded-md hover:bg-blue-700">Login</button>
        <p class="text-xs">Don't have an account? <a href="/register" class="font-bold text-blue-500">Register</a></p>
    </form>
</div>